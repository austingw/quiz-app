export default function Question(props) {
  return (
    <div className="question">
      {props.question}
      {props.answer}
      {props.answer}
      {props.answer}
      {props.answer}
    </div>
  );
}
